<!DOCTYPE html>
<html>
    <head>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css')}}"/>
    <link rel="short icon" href="{{ asset('images/logo.png') }}"><title>Voyage Voyages</title>
    </head>
    <body>
    <div>
    <p>Log : root@root</p>
    <p>password : 123456</p>
    <a href="{{ path('app_login') }}">Identification</a>
    </div>
    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
        <div class="d-flex">
        <a class="btn btn-primary ms-3" href="{{ path('voyage_index') }}">Gestion des voyages</a>
        <a class="btn btn-primary mx-5" href="{{ path('categorie_index') }}">Gestion des Categories</a>
        <a class="btn btn-primary mx-5" href="{{ path('tag_index') }}">Gestion des Tags</a>
        <a class="btn btn-primary me-3" href="{{ path('user_index') }}">Gestion des Utilisateurs</a>
        </div>

    {% endif %}
        {% block body %}{% endblock %}
    </body>
</html>
