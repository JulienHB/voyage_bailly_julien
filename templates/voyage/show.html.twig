{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}
    <h1>Voyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ voyage.nom }}</td>
            </tr>
            <tr>
                <th>Accroche</th>
                <td>{{ voyage.accroche }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voyage.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voyage.prix }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ voyage.duree }}</td>
            </tr>
            <tr>
                <th>Image1</th>
                <td>{{ voyage.image1 }}</td>
            </tr>
            <tr>
                <th>Image2</th>
                <td>{{ voyage.image2 }}</td>
            </tr>
            <tr>
                <th>Image3</th>
                <td>{{ voyage.image3 }}</td>
            </tr>
            <tr>
                <th>Brochure</th>
                <td>{{ voyage.brochure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('voyage_index') }}">back to list</a>

    <a href="{{ path('voyage_edit', {'id': voyage.id}) }}">edit</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
